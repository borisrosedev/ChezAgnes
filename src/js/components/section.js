const section = function(className, template){
    return(
        `
            <section class="${className}">

                ${template}
            
            </section>
        
        `
    )
}

export default section